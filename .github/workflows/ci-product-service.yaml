name: CI - product-service

on:
  push:
    branches: [main]
    paths:
      - 'product-service/**'

concurrency:
  group: git-push-values
  cancel-in-progress: false

jobs:
  call-build:
    uses: ./.github/workflows/_reusable-build.yaml
    with:
      service_name: product-service