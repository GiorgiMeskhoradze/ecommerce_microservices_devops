name: CI - discovery-server

on: 
  push: 
    branches: [main]
    paths:
      - 'discovery-server/**'
      
concurrency:
  group: git-push-values
  cancel-in-progress: false

jobs:
  call-build:
    uses: ./.github/workflows/_reusable-build.yaml
    with: 
      service_name: discovery-server

