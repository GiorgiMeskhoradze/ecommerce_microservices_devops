apiVersion: v1
kind: Service
metadata:
  name: zipkin
spec:
  type: {{ .Values.service.type }}
  selector:
    app: zipkin
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP