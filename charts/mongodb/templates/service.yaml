apiVersion: v1
kind: Service
metadata:
  name: mongodb
spec:
  ClusterIP: none
  selector:
    app: mongodb
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP